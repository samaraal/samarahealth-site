/* Public settings. The key below is Supabase's PUBLISHABLE key (safe to be public; the same one the ERP uses).
   This website only uses it to call website_submit_enquiry (send an enquiry). */
window.SAMARA_SITE_CONFIG = Object.freeze({
  supabaseUrl: 'https://askalabwtlrnoodinayq.supabase.co',
  supabaseKey: 'sb_publishable_MPf0spA1IsJWWR5-ltVAyA_Z_gctBr-',
  whatsapp: '917395961616'
});
